import React from 'react';
import { Text, View, SafeAreaView } from 'react-native';

export default function App() {
  return (
    <SafeAreaView>
      <View style={{ padding: 24 }}>
        <Text>React Native — Todo Gerarchica</Text>
        <Text>Questa è una base Expo/TS. Collega Firebase come nella web app.</Text>
      </View>
    </SafeAreaView>
  );
}
